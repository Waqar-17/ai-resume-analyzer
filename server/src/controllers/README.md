# Controllers Directory

This directory contains the business logic for your application. Controllers are called by routes and interact with models and utility functions to process requests and send responses.

Example: `analyzeController.js`
