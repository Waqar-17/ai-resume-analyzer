# Config Directory

This directory contains configuration files and setup logic for third-party services.

Example: `db.js` (for database connection), `cloudinary.js` (for image/PDF upload configuration).
